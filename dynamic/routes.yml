# This file defines all dynamic routing logic (routers & middlewares).
http:
  # ===================================================================
  #  MIDDLEWARES
  # ===================================================================
  middlewares:
    # --- Security Middlewares ---
    dash-auth:
      # Basic Auth for the dashboard (reads hashed user:pass from env).
      basicAuth:
        users:
          - '{{ env "TRAEFIK_DASHBOARD_AUTH" }}'

    secure-headers:
      # Applies best-practice security headers to responses.
      headers:
        frameDeny: true                 # Set X-Frame-Options to DENY.
        browserXssFilter: true          # Enable XSS filtering.
        contentTypeNosniff: true        # Prevent MIME-type sniffing.
        stsIncludeSubdomains: true      # HSTS: Include subdomains.
        stsPreload: true                # HSTS: Allow preloading.
        stsSeconds: 31536000            # HSTS: One-year cache.

    # --- Rate Limit Middlewares ---
    rate-limit-admin:
      # Rate limiting for administrative endpoints.
      rateLimit:
        average: 200                     # 200 requests/minute average.
        period: "1m"
        burst: 100                       # Allow bursts of 100 requests.

    rate-limit-api:
      # Rate limiting for public API endpoints.
      rateLimit:
        average: 100                    # 100 requests/minute average.
        period: "1m"
        burst: 50                       # Allow bursts of 50 requests.

  # ===================================================================
  #  ROUTERS
  # ===================================================================
  routers:
    # --- 1. Sirius Router (Frontend) ---
    sirius-router: # <- RENOMMÉ
      rule: "Host(`stellar.atlas`)"             # Match 'stellar.atlas'.
      entryPoints: [ "websecure" ]              # Use the 'websecure' (HTTPS) entrypoint.
      tls: true                                 # Enable TLS (HTTPS).
      priority: 5                               # Low priority to act as a catch-all for this domain.
      service: "sirius-app-service"             # Route to the frontend service.
      middlewares: [ "secure-headers" ]         # Apply security headers.

    # --- 2. Antares Routers (Proxy, Dedicated, Swagger) ---
    antares-proxy-router: # <- RENOMMÉ
      rule: "Host(`stellar.atlas`) && PathPrefix(`/api`)" # Match 'stellar.atlas/api...'.
      entryPoints: [ "websecure" ]
      tls: true
      priority: 10                              # High priority to catch '/api' before the frontend router.
      service: "antares-auth-service"           # Route to the 'antares-auth' service.
      middlewares: [ "rate-limit-api" ]         # Apply API rate limiting.

    antares-dedicated-router: # <- RENOMMÉ
      rule: "Host(`api.stellar.atlas`)"         # Match dedicated 'api.stellar.atlas' subdomain.
      entryPoints: [ "websecure" ]
      tls: true
      service: "antares-auth-service"
      middlewares: [ "rate-limit-api" ]

    antares-swagger-router: # <- RENOMMÉ
      # High-priority rule to catch Swagger/OpenAPI paths before the frontend router.
      rule: "Host(`stellar.atlas`) && (PathPrefix(`/swagger-ui`) || PathPrefix(`/v3/api-docs`))"
      entryPoints: [ "websecure" ]
      tls: true
      priority: 10
      service: "antares-auth-service"
      middlewares: [ "rate-limit-api" ]

    # --- 3. Vega Router (Spring Boot Admin) ---
    vega-router: # <- RENOMMÉ
      rule: "Host(`admin.stellar.atlas`)"       # Match 'admin.stellar.atlas'.
      entryPoints: [ "websecure" ]
      tls: true
      service: "vega-admin-service"          # <- CORRIGÉ (pointait vers 'antares-admin-service')
      middlewares: [ "rate-limit-admin" ]       # Apply administrative rate limiting.

    # --- 4. Traefik Dashboard Router ---
    traefik-dashboard-router: # <- RENOMMÉ
      rule: "Host(`dashboard.stellar.atlas`)"   # Match 'dashboard.stellar.atlas'.
      entryPoints: [ "websecure" ]
      tls: true
      service: "api@internal"                   # Route to Traefik's internal API/dashboard service.
      middlewares:
        - "dash-auth"                           # Secure with Basic Auth.
        - "rate-limit-admin"                    # Secure with rate limiting.

  # ===================================================================
  #  SERVICES
  # ===================================================================
  services:
    # --- 1. Frontend Application Service (Sirius) ---
    sirius-app-service: # <- RENOMMÉ
      loadBalancer:
        servers:
          # Points to the Nginx container serving the Angular app.
          - url: "http://sirius-app:80" # <- CORRIGÉ

    # --- 2. API Service (Antares) ---
    antares-auth-service: # <- RENOMMÉ
      loadBalancer:
        servers:
          # Points to the Spring Boot API container.
          - url: "http://antares-auth:8080" # <- CORRIGÉ

    # --- 3. Admin Service (Vega) ---
    vega-admin-service: # <- RENOMMÉ
      loadBalancer:
        servers:
          # Points to the Spring Boot Admin container.
          - url: "http://vega-admin:9091" # <- CORRIGÉ