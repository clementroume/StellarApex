# This file provides TLS certificates to Traefik for local development.
tls:
  certificates:
    # Paths are relative to inside the Traefik container (see docker-compose.yml volumes).
    - certFile: /certs/local.crt
      keyFile: /certs/local.key